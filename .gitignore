# Terraform / OpenTofu
.terraform
.terraform.lock.hcl
*terraform.tfstate*
*terraform.tfplan*
*.planfile
*.terraform.tfvars.json
backend.tf.json
kubeconfig
talosconfig

# Committed, encrypted local state (DR-in-git). Only these exact files are
# un-ignored; their plaintext *.backup siblings stay ignored.
!components/terraform/github/repositories/terraform.tfstate
!components/terraform/aws/bootstrap/dev.terraform.tfstate
!components/terraform/aws/bootstrap/root.terraform.tfstate
!components/terraform/aws/bootstrap/prod.terraform.tfstate

# IntelliJ IDEA
.idea
