# Terraform / OpenTofu
.terraform
.terraform.lock.hcl
*terraform.tfstate*
*terraform.tfplan*
*.planfile
*.terraform.tfvars.json
backend.tf.json

# github/repositories keeps a committed local state (no secrets)
!components/terraform/github/repositories/terraform.tfstate

# IntelliJ IDEA
.idea
