components:
  terraform:
    private-domain:
      metadata:
        component: private-domain
        terraform_workspace: default
      backend:
        s3:
          key: private-domain/terraform.tfstate
      vars:
        component: private-domain
