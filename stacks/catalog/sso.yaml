components:
  terraform:
    sso:
      metadata:
        component: sso
        terraform_workspace: default
      backend:
        s3:
          key: sso/terraform.tfstate
      vars:
        component: sso
