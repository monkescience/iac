components:
  terraform:
    cognito:
      metadata:
        component: cognito
        terraform_workspace: default
      backend:
        s3:
          key: cognito/terraform.tfstate
      vars:
        component: cognito
