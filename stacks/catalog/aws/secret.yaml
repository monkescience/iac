components:
  terraform:
    aws/secret:
      metadata:
        component: aws/secret
        terraform_workspace: default
      backend:
        s3:
          key: secret/terraform.tfstate
      vars:
        component: secret
