components:
  terraform:
    aws/vpn:
      metadata:
        component: aws/vpn
        terraform_workspace: default
      backend:
        s3:
          key: vpn/terraform.tfstate
      vars:
        component: vpn
