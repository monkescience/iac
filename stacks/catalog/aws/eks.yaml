components:
  terraform:
    aws/eks:
      metadata:
        component: aws/eks
        terraform_workspace: default
      backend:
        s3:
          key: eks/terraform.tfstate
      vars:
        component: eks
        eks_version: "1.34"
        eks_public_access_cidrs:
          - 62.224.83.150/32
