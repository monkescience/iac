components:
  terraform:
    bootstrap:
      metadata:
        component: bootstrap
        terraform_workspace: default
      backend:
        s3:
          key: bootstrap/terraform.tfstate
      vars:
        component: bootstrap
